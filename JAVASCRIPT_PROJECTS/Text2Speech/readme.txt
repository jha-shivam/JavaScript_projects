why getVoices give empty array when we normally call it?
answer: getvoices take time to populate array and when page loads it asynchrnously 
populate the array so at the page loading time it shows as a empty array.

solution: 
